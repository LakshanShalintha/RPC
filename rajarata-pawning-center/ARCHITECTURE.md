# 🏗️ System Architecture - Slider Management

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐         ┌──────────────────────┐   │
│  │   Admin Panel        │         │    Home Page         │   │
│  │  /admin/page.tsx     │         │  /home/page.tsx      │   │
│  │                      │         │                      │   │
│  │ • Upload Images      │         │ • Display Sliders    │   │
│  │ • Reorder Sliders    │         │ • Auto-rotation      │   │
│  │ • Delete Sliders     │         │ • Smooth transitions │   │
│  │ • Preview Grid       │         │ • Loading states     │   │
│  └──────────┬───────────┘         └──────────┬───────────┘   │
│             │                                 │               │
└─────────────┼─────────────────────────────────┼───────────────┘
              │                                 │
              │                                 │
              ▼                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │         SliderService (lib/sliderService.ts)           │   │
│  │                                                        │   │
│  │  getAllSliders()      → Fetch all slider records      │   │
│  │  addSlider(file)      → Upload & create record        │   │
│  │  deleteSlider(id,url) → Remove from storage & DB      │   │
│  │  updateSliderOrder()  → Change position               │   │
│  │  reorderSliders()     → Batch position update         │   │
│  └────────────────────────┬───────────────────────────────┘   │
│                           │                                    │
│  ┌────────────────────────┴───────────────────────────────┐   │
│  │         Supabase Client (lib/supabase.ts)             │   │
│  │                                                        │   │
│  │  • Connection Config                                  │   │
│  │  • Type Definitions                                   │   │
│  │  • Environment Variables                              │   │
│  └────────────────────────┬───────────────────────────────┘   │
│                           │                                    │
└───────────────────────────┼────────────────────────────────────┘
                            │
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                       SUPABASE BACKEND                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────┐  ┌──────────────────────┐   │
│  │   PostgreSQL Database        │  │   Storage Bucket     │   │
│  │                              │  │                      │   │
│  │  ┌────────────────────────┐ │  │  slider-images/      │   │
│  │  │  sliders table         │ │  │    sliders/          │   │
│  │  │                        │ │  │      • image1.jpg    │   │
│  │  │ • id (PK)              │ │  │      • image2.png    │   │
│  │  │ • image_url (TEXT)     │ │  │      • image3.webp   │   │
│  │  │ • order_index (INT)    │ │  │                      │   │
│  │  │ • created_at (TIMESTAMP)│ │  │   Public Access ✅   │   │
│  │  │                        │ │  │                      │   │
│  │  │ RLS Policies:          │ │  │  Storage Policies:   │   │
│  │  │  ✓ Public Read         │ │  │   ✓ Public SELECT   │   │
│  │  │  ✓ Auth Insert/Update  │ │  │   ✓ Auth INSERT     │   │
│  │  │  ✓ Auth Delete         │ │  │   ✓ Auth DELETE     │   │
│  │  └────────────────────────┘ │  │                      │   │
│  └──────────────────────────────┘  └──────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. Upload Image Flow

```
User (Admin Panel)
    │
    │ 1. Select image file
    │
    ▼
┌───────────────┐
│  File Input   │
└───────┬───────┘
        │
        │ 2. Validate (size, type)
        │
        ▼
┌────────────────────┐
│  SliderService     │
│  addSlider()       │
└──────┬─────────────┘
       │
       │ 3. Upload to Storage
       │
       ▼
┌──────────────────────┐
│ Supabase Storage     │
│ slider-images/sliders│
└──────┬───────────────┘
       │
       │ 4. Get public URL
       │
       ▼
┌─────────────────────┐
│ Create DB Record    │
│ (id, url, order)    │
└──────┬──────────────┘
       │
       │ 5. Return success
       │
       ▼
┌──────────────────────┐
│ Update UI            │
│ Show success message │
│ Refresh slider list  │
└──────────────────────┘
```

---

### 2. Display Sliders Flow (Home Page)

```
User (Home Page)
    │
    │ 1. Navigate to /home
    │
    ▼
┌────────────────────┐
│  HomePage          │
│  useEffect()       │
└──────┬─────────────┘
       │
       │ 2. Fetch sliders
       │
       ▼
┌────────────────────┐
│ SliderService      │
│ getAllSliders()    │
└──────┬─────────────┘
       │
       │ 3. Query database
       │
       ▼
┌──────────────────────┐
│ Supabase Database    │
│ SELECT * FROM sliders│
│ ORDER BY order_index │
└──────┬───────────────┘
       │
       │ 4. Return slider data
       │
       ▼
┌──────────────────────┐
│ Set State (slides)   │
└──────┬───────────────┘
       │
       │ 5. Auto-rotation (4s)
       │
       ▼
┌──────────────────────┐
│ Display with fade    │
│ transitions          │
└──────────────────────┘
```

---

### 3. Delete Slider Flow

```
User (Admin Panel)
    │
    │ 1. Click "Delete"
    │
    ▼
┌────────────────────┐
│ Confirm Dialog     │
└──────┬─────────────┘
       │
       │ 2. User confirms
       │
       ▼
┌────────────────────┐
│ SliderService      │
│ deleteSlider()     │
└──────┬─────────────┘
       │
       ├─── 3a. Delete from Storage
       │
       ▼
┌──────────────────────┐
│ Supabase Storage     │
│ DELETE file          │
└──────┬───────────────┘
       │
       ├─── 3b. Delete from Database
       │
       ▼
┌──────────────────────┐
│ Supabase Database    │
│ DELETE FROM sliders  │
│ WHERE id = ?         │
└──────┬───────────────┘
       │
       │ 4. Return success
       │
       ▼
┌──────────────────────┐
│ Update UI            │
│ Remove from list     │
│ Show success message │
└──────────────────────┘
```

---

### 4. Reorder Sliders Flow

```
User (Admin Panel)
    │
    │ 1. Click "Move Up/Down"
    │
    ▼
┌────────────────────┐
│ Calculate new      │
│ positions          │
└──────┬─────────────┘
       │
       │ 2. Update order
       │
       ▼
┌────────────────────┐
│ SliderService      │
│ updateSliderOrder()│
└──────┬─────────────┘
       │
       │ 3. Update database
       │
       ▼
┌──────────────────────┐
│ Supabase Database    │
│ UPDATE sliders       │
│ SET order_index = ?  │
│ WHERE id = ?         │
└──────┬───────────────┘
       │
       │ 4. Return success
       │
       ▼
┌──────────────────────┐
│ Update local state   │
│ Reorder list         │
│ Show success message │
└──────────────────────┘
```

---

## Component Hierarchy

```
App (layout.tsx)
│
├── Admin Panel (/admin/page.tsx)
│   │
│   ├── Header
│   │   ├── Title
│   │   └── Language Toggle
│   │
│   ├── Upload Section
│   │   ├── File Input
│   │   └── Validation Info
│   │
│   ├── Messages
│   │   ├── Error Alert
│   │   ├── Success Alert
│   │   └── Loading Alert
│   │
│   └── Slider Grid
│       └── Slider Card (for each slider)
│           ├── Image Preview
│           ├── Order Badge
│           ├── Move Up Button
│           ├── Move Down Button
│           ├── Delete Button
│           └── Timestamp
│
└── Home Page (/home/page.tsx)
    │
    ├── Hero Section
    │   ├── Left: Text Content
    │   │   ├── Title
    │   │   ├── Subtitle
    │   │   └── Icon Cards
    │   │
    │   └── Right: Slider Container
    │       └── Slider Images (overlapping with fade)
    │
    └── Other Sections
        ├── Stats
        ├── Services
        └── ...
```

---

## File Dependencies

```
Admin Panel (page.tsx)
    ├── imports SliderService
    ├── imports Slider type
    ├── imports useLanguage
    └── imports React hooks

Home Page (page.tsx)
    ├── imports SliderService
    ├── imports Slider type
    ├── imports useLanguage
    └── imports React hooks

SliderService (sliderService.ts)
    ├── imports supabase client
    └── imports Slider type

Supabase Client (supabase.ts)
    ├── imports @supabase/supabase-js
    ├── uses NEXT_PUBLIC_SUPABASE_URL
    └── uses NEXT_PUBLIC_SUPABASE_ANON_KEY

Environment (.env.local)
    ├── NEXT_PUBLIC_SUPABASE_URL
    └── NEXT_PUBLIC_SUPABASE_ANON_KEY
```

---

## API Endpoints (Optional Routes)

```
GET /api/sliders
├── Returns: Array<Slider>
├── Use: Fetch all sliders
└── Alternative: Direct client call

POST /api/sliders
├── Body: FormData with file
├── Returns: Slider object
├── Use: Upload new slider
└── Alternative: Direct client call

DELETE /api/sliders?id={id}&imageUrl={url}
├── Params: id, imageUrl
├── Returns: Success boolean
├── Use: Delete slider
└── Alternative: Direct client call
```

---

## Security Layers

```
┌─────────────────────────────────────────────┐
│        PUBLIC ACCESS (Current)              │
│  • Anyone can view sliders (home page)      │
│  • Anyone can manage sliders (admin)        │
└─────────────────────────────────────────────┘
                    │
                    │ Add Authentication
                    ▼
┌─────────────────────────────────────────────┐
│      AUTHENTICATED ACCESS (Recommended)      │
│  • Public can view sliders (home page)      │
│  • Only admins can manage (login required)  │
└─────────────────────────────────────────────┘
                    │
                    │ Add Authorization
                    ▼
┌─────────────────────────────────────────────┐
│        ROLE-BASED ACCESS (Advanced)         │
│  • Public: View only                        │
│  • Admin: Full CRUD                         │
│  • Super Admin: Everything + settings       │
└─────────────────────────────────────────────┘
```

---

## Technology Stack

```
Frontend
├── Next.js 16.0.1 (React 19.2.0)
├── TypeScript
├── Tailwind CSS 4.1.16
└── Framer Motion 12.23.24

Backend (BaaS)
├── Supabase (PostgreSQL)
├── Supabase Storage
└── Supabase Auth (optional)

Deployment
├── Vercel (recommended)
├── Netlify (alternative)
└── Custom server (advanced)
```

---

## Environment Configuration

```
Development
├── .env.local (local settings)
├── Local Supabase instance (optional)
└── Dev database

Production
├── Environment variables (Vercel/Netlify)
├── Production Supabase project
└── Production database
```

---

## Scaling Considerations

```
Current Capacity
├── Unlimited sliders (database limit)
├── 1GB free storage (Supabase)
└── 50GB bandwidth/month (Supabase free tier)

Future Scaling
├── Add CDN (Cloudflare)
├── Image optimization (Next.js Image)
├── Caching layer (Redis)
└── Upgrade Supabase plan
```

---

This architecture provides:
✅ Clear separation of concerns
✅ Scalable structure
✅ Easy to maintain
✅ Type-safe operations
✅ Error handling at each layer
✅ Security ready (add auth when needed)
